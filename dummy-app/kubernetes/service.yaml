apiVersion: v1
kind: Service
metadata:
  name: node-typescript-app
  labels:
    app: node-typescript-app
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: node-typescript-app
