services:
  k6:
    image: grafana/k6:latest
    network_mode: "host"
    volumes:
      - ./dist:/scripts
    environment:
      - API_URL=${API_URL:-http://localhost:3000}
    command: run /scripts/script.js
